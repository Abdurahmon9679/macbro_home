import 'dart:ui';

class AppColors {
  /// home page background
  static const Color homeBackColor = Color(0xffF9F9FD);

  static const List<Color> gradient = [
    Color(0xff5BA9FF),
    blue,
    whiteColor,
  ];

  /// for secondary color for icon and text
  static const Color greyColor = Color(0xffABABAB);

  /// like button background
  static const Color likeBtnBack = Color(0xffF5F5F5);

  /// appbar shadow
  static const Color appbarShadow = Color.fromRGBO(0, 0, 0, 0.05);

  /// background color
  static const Color whiteColor = Color(0xffFFFFFF);

  /// title color
  static const Color primary = Color(0xff000000);

  /// for price an active icon
  static const Color blue = Color(0xff007AFF);
}
